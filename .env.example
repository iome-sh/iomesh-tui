# Copy to .env (never commit). Loaded manually by the shell / direnv if desired.
# iomesh reads process environment; it does not auto-load .env files yet.

DEEPSEEK_API_KEY=
XAI_API_KEY=
IOMESH_API_KEY=
IOMESH_ENDPOINT=
IOMESH_TENANT=
IOMESH_DEFAULT_MODEL=
IOMESH_SUBAGENTS=1
# IOMESH_YOLO=0
